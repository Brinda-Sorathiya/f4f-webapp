import orderMod from "../models/ordermod.js";
import userMod from "../models/usermod.js";
import Stripe from "stripe";

const palaceOrder = async (req.res) => {
    
}

export default palaceOrder